export const metadata = {
  title: "Login | ITELECT",
  description: "Page made",
};

export default function LoginLayout({ children }) {
  return (
    <html lang="en">
    <body>
        <>{children}</>
    </body>
    </html>
  );
}